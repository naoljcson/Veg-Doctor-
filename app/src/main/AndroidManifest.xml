<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vegdoc">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VegDoc"
        tools:targetApi="31">
        <activity
            android:name=".view.DetailInformationActivity"
            android:exported="false"
            android:label="@string/title_activity_dis_order_list"
            android:theme="@style/Theme.VegDoc.NoActionBar" />
        <activity
            android:name=".view.DisOrderListActivity"
            android:exported="false"
            android:label="@string/title_activity_detail_information"
            android:theme="@style/Theme.VegDoc.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VegDoc.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>